name: Elixir - Test

on:
  workflow_call:
    inputs:
      elixirs:
        type: string
        default: "[\"1.15.0\", \"1.16.x\", \"1.17.x\", \"1.18.x\", \"1.19.x\"]"
      erlangs:
        type: string
        default: "[\"26.x\", \"27.x\", \"28.x\"]"

env:
  MIX_ENV: test

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        elixir: ${{ fromJSON(inputs.elixirs) }}
        otp: ${{ fromJSON(inputs.erlangs) }}

    steps:
    - name: Checkout code
      uses: actions/checkout@v5

    - name: Setup Elixir
      uses: erlef/setup-beam@v1
      with:
        elixir-version: ${{ matrix.elixir }}
        otp-version: ${{ matrix.otp }}

    - name: Disable compile warnings
      run: echo "::remove-matcher owner=elixir-mixCompileWarning::"

    - name: Retrieve mix dependencies cache
      uses: actions/cache@v4
      id: mix-cache
      with:
        path: |
          deps
          _build
        key: test-${{ runner.os }}-${{ matrix.otp }}-${{ matrix.elixir }}-${{ hashFiles('**/mix.lock') }}
        restore-keys: test-${{ runner.os }}-${{ matrix.otp }}-${{ matrix.elixir }}-

    - name: Install mix dependencies
      if: steps.mix-cache.outputs.cache-hit != 'true'
      run: |
        mix deps.get
        mix deps.compile

    - name: Run tests
      run: mix test
